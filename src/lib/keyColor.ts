import type { KeyColor } from "@/content/types";

export const KEY_COLOR_VAR: Record<KeyColor, string> = {
  sky: "var(--brand-sky)",
  orchid: "var(--brand-orchid)",
  blush: "var(--brand-blush)",
};

export const KEY_COLOR_SOFT_VAR: Record<KeyColor, string> = {
  sky: "var(--brand-ice)",
  orchid: "var(--brand-petal)",
  blush: "var(--brand-petal)",
};
