# Seoulmeguri — ランディングサイト 仕様書 (spec.md)

> 일본인 여행객을 위한 서울 여행 정보 사이트
> 작성일: 2026-08-13 / v0.1

---

## 1. 프로젝트 개요

### 1.1 정체성

| 항목 | 내용 |
|---|---|
| 사이트명 | **Seoulmeguri** (ソウルめぐり) |
| 인스타그램 | [@seoulmeguri](https://www.instagram.com/seoulmeguri/) |
| 타깃 | 서울 여행을 계획 중인 일본인 여성, 20〜30대 중심 |
| 언어 | **일본어 단일** (한국어·영어 대응 없음) |
| 한 줄 정의 | 「どこに行くか」ではなく「**いつ・どの順番で**行くか」を教えてくれるソウルガイド |

### 1.2 왜 이 사이트가 필요한가 (경쟁 분석 결론)

일본어 서울 정보는 이미 포화 상태다. 코네스트, 크리에이트립, asoview, Funliday가 모든 지역을 두껍게 덮고 있다. 같은 게임으로는 이길 수 없다.

경쟁사 콘텐츠를 감사한 결과, **구조적 빈틈은 두 가지**다.

**① 「曜日」축이 없다**
기존 매체는 스팟 나열은 하지만 "월요일에 가면 절반이 닫혀 있다"를 말하지 않는다. 시청·정동은 월요일에 덕수궁·서울시립미술관·서울도서관이 전부 휴관이고 수문장 교대식도 없다. 한남동은 일요일에 갤러리가 대부분 닫힌다. 이걸 정면으로 다루는 일본어 페이지가 사실상 없다.

**② 정보가 낡았는데 갱신되지 않는다**
가로수길 공실률은 43.9%(서울 주요 상권 중 최고)인데, 일본어 강남 가이드는 여전히 가로수길을 1번으로 소개한다. 크리에이트립조차 예약 상품과 연결되는 주제(환전·한복)에만 리소스를 쓰고 나머지는 방치한다.

**→ Seoulmeguri의 포지션: 「行ける日・行けない日」と「今の正しい情報」を言い切るサイト**

### 1.3 v1 스코프

**포함**
- 랜딩 페이지 (톱)
- 에리어 코스 3개: 市庁・貞洞 / 漢南 / 江南
- 旅の準備 기사 1개: 両替ガイド
- 曜日チェッカー (시그니처 기능)
- Instagram 연동

**제외 (v2 이후)**
- 성수·홍대·여의도·삼청 코스
- 美容(ヘア・パーソナルカラー)専用ページ ※의료광고 규제 검토 필요
- 검색 / 필터 UI
- CMS / 로그인 / 예약

---

## 2. 디자인 시스템

### 2.1 컬러 토큰

첨부된 팔레트(Pastel Dreamland Adventure)를 브랜드 컬러로 채택. 단 **5색 모두 명도가 높아 텍스트에 사용 불가**하므로, 잉크·CTA·링크용 파생 토큰을 추가한다.

#### 브랜드 컬러 (면·배경·액센트 전용)

| 토큰 | HEX | 용도 |
|---|---|---|
| `--brand-orchid` | `#CDB4DB` | 에리어「漢南」키컬러 / 대형 면 |
| `--brand-petal` | `#FFC8DD` | 카드 배경 / 부드러운 구분 |
| `--brand-blush` | `#FFAFCC` | 에리어「江南」키컬러 / 강조 |
| `--brand-ice` | `#BDE0FE` | 정보 박스 / 배경 |
| `--brand-sky` | `#A2D2FF` | 에리어「市庁・貞洞」키컬러 |

#### 파생 토큰 (필수 추가)

| 토큰 | HEX | 용도 |
|---|---|---|
| `--ink` | `#2B2130` | 본문·제목 텍스트 (딥 플럼블랙) |
| `--ink-2` | `#6E6377` | 캡션·보조 텍스트 |
| `--ink-3` | `#A79FAE` | 비활성·플레이스홀더 |
| `--paper` | `#FFFCFE` | 기본 배경 |
| `--paper-2` | `#F8F3F9` | 섹션 교차 배경 |
| `--accent` | `#D6407C` | CTA 버튼 / 중요 액션 (Blush Pop 파생) |
| `--accent-ink` | `#FFFFFF` | CTA 위 텍스트 |
| `--link` | `#2E6FB7` | 텍스트 링크 (Sky Blue 파생) |
| `--warn` | `#B54708` | 「行けない日」경고 |
| `--line` | `#EDE4F0` | 보더 |

> **접근성 규칙**
> - 브랜드 5색 위에는 `--ink` 만 올린다. 흰 텍스트 금지.
> - `--accent`, `--link`, `--warn`은 빌드 시 WCAG AA(4.5:1) 자동 검증할 것.
> - 상태를 **색만으로** 표현하지 않는다. 曜日 뱃지는 반드시 아이콘+텍스트 병기.

### 2.2 타이포그래피

일본어 사이트이므로 JP 폰트 선정이 디자인의 절반이다.

| 역할 | 폰트 | 이유 |
|---|---|---|
| Display (見出し) | **Zen Maru Gothic** 700 | 丸ゴシック. 파스텔 팔레트와 톤이 맞고, Noto/游ゴシック 같은 기본값이 아니다 |
| Body (本文) | **Zen Kaku Gothic New** 400/500 | 같은 Zen 패밀리라 조화. 장문 가독성이 Maru보다 좋다 |
| Utility (時刻・数字) | **Outfit** 500 | 시간·요일·번호 전용. 라틴 숫자를 또렷하게 |

**타입 스케일**

```
display-l   40 / 1.35  Zen Maru Gothic 700   letter-spacing: 0.02em
display-m   28 / 1.4   Zen Maru Gothic 700
title       20 / 1.5   Zen Maru Gothic 700
body        16 / 1.9   Zen Kaku Gothic New 400   ← 日本語は行間を広めに
body-s      14 / 1.8   Zen Kaku Gothic New 400
caption     12 / 1.6   Zen Kaku Gothic New 500
data        14 / 1.2   Outfit 500  (時刻・曜日)
```

**일본어 조판 규칙**
- `line-height` 는 라틴보다 넓게 (본문 1.9). 좁으면 한자가 뭉친다.
- 금칙처리: `word-break: normal; overflow-wrap: anywhere;` + 주요 제목은 `<wbr>` 또는 `budoux` 로 문절 단위 개행
- 볼드 남용 금지. 일본어 고딕은 볼드가 강해서 페이지가 시끄러워진다

⚠️ **폰트 로딩 주의**
JP 폰트는 풀셋이 수 MB다. `next/font/google` 은 셀프호스팅 시 전체를 받으므로 JP에는 부적합할 수 있다. 다음 중 하나를 채택하고 실측할 것.
1. Google Fonts CSS를 `<link>`로 로드 (unicode-range 동적 서브셋이 적용됨) — 권장
2. `fontsource` 서브셋 파일 사용
3. 사용 문자 기반 정적 서브셋 생성 (콘텐츠 고정형이므로 가능)

목표: **LCP < 2.5s (4G, 모바일)**

### 2.3 레이아웃

**모바일 퍼스트.** 일본인 여행객은 현지에서 스마트폰으로 본다. 데스크톱은 부차적.

```
브레이크포인트   sm 480 / md 768 / lg 1024
콘텐츠 최대폭    720px (읽기 중심 사이트이므로 좁게)
그리드          모바일 1col / md 이상 12col
여백 스케일      4 / 8 / 12 / 16 / 24 / 32 / 48 / 64 / 96
라운드          카드 20px / 버튼 999px / 이미지 16px
그림자          사용 최소화. 대신 --line 보더와 --paper-2 면 대비로 층을 만든다
```

### 2.4 시그니처 요소

디자인의 대담함은 **한 곳에만** 쓴다. 나머지는 조용하게.

#### ①「めぐりライン」— 사이트의 얼굴

"めぐり(순회)"라는 이름을 그대로 시각화한다. 코스 페이지의 스팟들을 **손그림 느낌의 파스텔 곡선**이 세로로 꿰뚫는다.

```
  ●━━━━━━━━━━━━━━━━━━  10:00  徳寿宮
  ┃
  ╰─╮
    ●━━━━━━━━━━━━━━━━  11:00  守門将交代式
      ┃
    ╭─╯
  ●━━━━━━━━━━━━━━━━━━  11:40  聖公会ソウル大聖堂
```

- SVG path, 스크롤 진입 시 `stroke-dashoffset` 로 그려지는 애니메이션
- 선의 색은 에리어 키컬러 (市庁=Sky / 漢南=Orchid / 江南=Blush)
- `prefers-reduced-motion` 시 즉시 완성 상태로 표시

#### ②「曜日チェッカー」— 사이트의 논리

톱 히어로 바로 아래. 사이트의 존재 이유를 첫 화면에서 증명하는 장치.

```
┌──────────────────────────────────┐
│  今日は何曜日？                     │
│  [月][火][水][木][金][土][日]        │
│                                  │
│  月曜日 → こうなります              │
│  ✕ 市庁・貞洞   休館だらけ。今日は避けて │
│  ○ 漢南        リウム休館。買い物ならOK │
│  ◎ 江南        ほぼ全部開いてます      │
└──────────────────────────────────┘
```

- 초기값은 클라이언트 시각 기준 오늘 (KST)
- 상태 3단계: `◎ ばっちり` / `○ 一部休み` / `✕ 今日はやめとこ`
- 색만이 아니라 기호+텍스트 병기

> 이 두 가지 외에 장식적 애니메이션은 넣지 않는다. 파스텔 + 과한 모션은 즉시 "AI가 만든 사이트"로 읽힌다.

---

## 3. 문체 가이드 (일본어 카피)

### 3.1 톤

**20대 여성이 친구에게 알려주는 톤.** 매체가 아니라 사람의 목소리.

| する | しない |
|---|---|
| です・ます 기반, 종종 체언止め로 리듬 | だ・である 조 |
| 「〜かも」「〜だと思う」로 여지를 남김 | 단정적 광고 문구 |
| 문장 짧게. 1문 40자 이내 목표 | 장문 설명 |
| 이모지는 섹션당 최대 1개 | 이모지 남발 |
| 「私」の視点 (実際に行った人の口ぶり) | 「当サイトでは」등 매체 어투 |
| 具体的な数字・時刻 | 「たくさん」「とても」 |

**避けたい表現**: 「〜しちゃう♡」「〜すぎる件」「〜しか勝たん」 — 유행이 지났거나 과잉이라 오히려 촌스럽게 읽힌다.

### 3.2 카피 샘플

**히어로**
```
ソウル、何曜日に行く？

行きたいお店が休みだった。
そんな残念な一日をなくすために作りました。
エリアごとの「行ける日」と「行けない日」、
ぜんぶ書いてます。
```

**曜日チェッカー 見出し**
```
月曜日のソウル、けっこう閉まってます
```

**에리어 카드 (市庁)**
```
市庁・貞洞
歩いて回れる、100年前のソウル

宮殿とレンガの洋館が同じ道に並んでる不思議なエリア。
半日あれば十分だから、明洞に泊まる人の
「午前中どうしよう」にちょうどいいです。

⚠️ 月曜はほぼ全滅。火〜日に行ってね
```

**에리어 카드 (漢南)**
```
漢南
聖水は人が多すぎる、と思ったら

ギャラリーとカフェとショップが徒歩圏にぎゅっと。
人に押されずに歩けるのがいちばんの魅力かも。
坂が多いので、ヒールはやめておいたほうが安全です。

⚠️ ギャラリーは日曜休みが多め。水〜土がベスト
```

**에리어 카드 (江南)**
```
江南
カロスキルは、いま行かなくて大丈夫

空き店舗が4割を超えていて、正直さびしい状態。
代わりに狎鴎亭ロデオと島山公園のほうへ。
今の江南はこっちに移りました。

◎ 月曜でもほぼ全部開いてます
```

**경고 박스 (북촌 레드존)**
```
知らないと10万ウォン取られます

北村韓屋村の一部は「レッドゾーン」に指定されていて、
観光目的で入れるのは 10:00〜17:00 のみ。
時間外に写真を撮っていると過怠料10万ウォンの対象です。
住民の方が実際に暮らしている場所なので、静かに歩きましょう。
```

**辛さ警告 (무교동 낙지)**
```
辛さレベル ★★★★☆

「辛いの得意」な日本人でもけっこうやられます。
心配なら、注文のときにこの一言を。

  덜 맵게 해주세요
  (トル メッケ ヘジュセヨ / 辛さ控えめでお願いします)
```

**空の状態 (曜日チェッカーで全滅の日)**
```
月曜日は、正直おすすめできる日じゃないです。
でも江南と聖水なら大丈夫。そっちに振りましょう。
```

---

## 4. 정보 구조

### 4.1 사이트맵

```
/                          トップ (ランディング)
/areas/[slug]              エリア別コース
  /areas/city-hall         市庁・貞洞
  /areas/hannam            漢南
  /areas/gangnam           江南
/guides/[slug]             旅の準備
  /guides/exchange         両替ガイド
/about                     このサイトについて
```

### 4.2 톱 페이지 구성

| # | 섹션 | 내용 | 비고 |
|---|---|---|---|
| 1 | Hero | 카피 + めぐりライン 모티프 | 큰 사진 1장 or SVG |
| 2 | **曜日チェッカー** | 요일 선택 → 3에리어 가부 | 시그니처. 접힘 금지 |
| 3 | エリアを選ぶ | 코스 카드 3장 | 각 카드에 키컬러+요일뱃지 |
| 4 | 旅の準備 | 両替 기사 카드 | v1은 1개, 리스트 구조는 미리 |
| 5 | Instagram | 최근 게시물 6장 그리드 | 없으면 링크 배너로 대체 |
| 6 | Footer | 사이트 소개 / 갱신일 / 免責 | |

### 4.3 에리어 페이지 구성 (3페이지 공통 서식)

이 순서를 3개 지역 모두 동일하게 유지한다. 서식이 곧 브랜드다.

| # | 섹션 | 내용 |
|---|---|---|
| 1 | エリアヒーロー | 이름 + 한 줄 정의 + 키컬러 면 |
| 2 | **こんな人におすすめ / おすすめしない** | 타깃 필터. 여기서 안 맞는 사람을 걸러낸다 |
| 3 | **曜日表** | 施設×曜日 매트릭스. 이 사이트의 핵심 |
| 4 | 行き方 | 最寄駅・出口・所要時間 |
| 5 | **1日コース** | めぐりライン + 스팟 카드 |
| 6 | ⚠️ このエリアの落とし穴 | 경고 박스 (요금·시간제한·辛さ·坂道 등) |
| 7 | 時間が余ったら | 대체 스팟 |
| 8 | 更新日 + 出典注記 | 신뢰도 장치 |

> §2「おすすめしない」은 일반 매체가 절대 안 쓰는 항목이다. 漢南에는 전통 한식이 거의 없다는 식의 정직한 필터가 오히려 신뢰를 만든다.

---

## 5. 데이터 모델

콘텐츠는 v1에서 **TypeScript 파일로 관리** (CMS 없음). 타입 안전성과 빌드 속도 우선.

```ts
// content/types.ts

export type Weekday = 'mon'|'tue'|'wed'|'thu'|'fri'|'sat'|'sun';
export type OpenState = 'good' | 'partial' | 'closed';  // ◎ / ○ / ✕

export interface Area {
  slug: string;
  nameJa: string;              // 市庁・貞洞
  nameKo: string;              // 시청·정동
  reading: string;             // シチョン・チョンドン
  tagline: string;             // 歩いて回れる、100年前のソウル
  keyColor: 'sky'|'orchid'|'blush';
  heroImage: string;

  station: {
    line: string;              // 地下鉄1・2号線
    name: string;              // 市庁駅
    exit: string;              // 2番出口
  };

  goodFor: string[];           // こんな人におすすめ
  notFor: string[];            // おすすめしない ← 重要
  weekday: Record<Weekday, OpenState>;
  weekdayNote: string;         // 月曜はほぼ全滅。火〜日に行ってね
  pitfalls: Pitfall[];
  stops: Stop[];
  extras: Stop[];              // 時間が余ったら
  updatedAt: string;           // ISO
}

export interface Stop {
  time: string;                // "10:00"
  nameJa: string;
  nameKo: string;              // 地図アプリ検索用。必ず併記
  blurb: string;
  tips?: string[];
  hours?: string;
  closedDays?: Weekday[];
  fee?: string;                // "1,000ウォン"
  reservation?: 'required' | 'recommended' | 'none';
  spiceLevel?: 1|2|3|4|5;      // 飲食店のみ
  naverMapUrl?: string;        // Googleマップは韓国で機能が制限される
  googleMapsUrl?: string;
  walkFromPrev?: string;       // "徒歩7分"
}

export interface Pitfall {
  severity: 'info' | 'warn' | 'danger';
  title: string;
  body: string;
}

export interface Guide {
  slug: string;
  title: string;
  summary: string;
  category: 'money' | 'transport' | 'entry' | 'manner';
  sections: GuideSection[];
  updatedAt: string;
}
```

### 5.1 지도 링크 정책

**Googleマップは韓国で経路検索が使えない。** 일본인이 가장 자주 겪는 사고다.

- 각 스팟에 `naverMapUrl` 을 **필수**로 넣는다
- 지도 버튼 라벨: `NAVERマップで開く`
- 톱 또는 両替ガイド 부근에 「韓国でGoogleマップが使えない理由」 짧은 안내를 배치

---

## 6. 콘텐츠 (v1 확정분)

### 6.1 市庁・貞洞

```
tagline: 歩いて回れる、100年前のソウル
station: 地下鉄1・2号線 市庁駅 2番出口
keyColor: sky

曜日表
  施設              月  火  水  木  金  土  日
  徳寿宮            ✕   ○   ○   ○   ○   ○   ○
  守門将交代式       ✕   ○   ○   ○   ○   ○   ○   (11:00/14:00/15:30・雨天中止)
  ソウル市立美術館    ✕   ○   ○   ○   ○   ○   ○
  ソウル図書館       ✕   ○   ○   ○   ○   ○   ○
  貞洞展望台        13:30〜 / 土日は9:00〜 / 祝日休み

コース
  10:00  徳寿宮                大人1,000ウォン・韓服なら無料
  11:00  大漢門 守門将交代式      約20分・無料
  11:40  聖公会ソウル大聖堂       11:00-16:00のみ開放
  12:20  北倉洞スンドゥブ 本店     英語メニューあり
  13:40  貞洞展望台             無料・カフェ併設
  14:40  徳寿宮石垣道・貞洞キル    11月上旬が銀杏の見頃
  15:10  ソウル市立美術館         入場無料
  16:30  貞洞第一教会           外観のみで十分
  17:10  ソウル図書館(旧市庁舎)    3階の旧市長室が保存されている
  18:30  乙支路ノガリ横丁         徒歩20分・21時以降が本番

落とし穴
  danger  月曜日はこのコースが成立しません
  info    展望台は曜日で使い分け(平日午前はソウル市庁9階の空展望台)
  warn    武橋洞タコは相当辛い
```

### 6.2 漢南

```
tagline: 聖水は人が多すぎる、と思ったら
station: 地下鉄6号線 漢江鎮駅 1番出口
keyColor: orchid

曜日表
  施設                 月  火  水〜土  日
  リウム美術館          ✕   ○   ○      ○
  ペースギャラリー       ✕   ○   ○      ✕
  タデウス・ロパック      展示期間中のみ・要確認
  ロギ(ディナー)        ✕   ✕   ○      ○
  ショップ・カフェ       ○   ○   ○      ○
  → ベストは水〜土

コース
  10:00  リウム美術館           要事前予約・坂道あり
  12:15  イタルリグクシ漢南       韓×伊フュージョン
  13:45  ペースギャラリー         入場無料
  14:45  タデウス・ロパック ソウル   徒歩15分
  16:00  ブルーボトル漢南
  17:00  マルディメクルディ漢南     19:00閉店
  18:30  ロギ漢南               要予約

落とし穴
  danger  月曜全滅・日曜はギャラリーが閉まる
  warn    坂が多い。ヒール不可
  info    伝統的な韓国料理はほぼない。サムギョプサル目当てなら他エリアへ
  info    リウムのデジタルガイドは身分証を預ける必要あり(パスポート)
```

### 6.3 江南

```
tagline: カロスキルは、いま行かなくて大丈夫
station: 水仁盆唐線 狎鴎亭ロデオ駅 5・6番出口
keyColor: blush

曜日表
  ほぼ全施設が年中無休。月曜に予定が崩れたときの受け皿になるエリア。
  ※宣陵・靖陵のみ月曜休み

コース
  11:00  狎鴎亭ロデオ通り
  12:00  ザ・ギワ(昼食)          タブレット注文・マッコリ多数
  13:30  コゼット島山            薬菓が名物・平日12:00開店
  14:30  ハウス島山              ジェントルモンスター/タンバリンズ/ヌデイク
  15:40  島山公園
  17:00  奉恩寺                 05:00-22:00・タクシー15分
  18:15  ピョルマダン図書館        10:30-22:00

落とし穴
  info    江南は「ひとつの街」ではない。江南駅/狎鴎亭・島山/三成COEXは徒歩でつながらない
  warn    カロスキルは空室率43.9%。日本語ガイドの情報は古い
  info    江南駅周辺は観光目的なら行く理由が薄い
```

### 6.4 両替ガイド

```
title: ウォンへの両替、どこがいちばん得？
category: money

セクション
  1. 結論から
     - 明洞が必ずしも最安ではない。移動時間と交通費を含めて考える
     - 全額を日本で両替する必要はない
  2. 場所別の比較
     - 仁川空港の銀行 / 明洞の両替所 / 弘大・東大門・江南 / セルフ両替機
  3. エリア別 両替所リスト
     - 明洞(乙支路入口6番出口・明洞5/6番出口周辺に集中)
     - 弘大 / 東大門 / 江南
     ※営業時間は変動するため「訪問前確認」を明記
  4. 両替のコツ
     - 1万円札でまとめる。破れ・書き込みのある紙幣は断られることがある
     - レート表示が「1円あたり」か「100円あたり」かを確認
     - 受け取ったウォンはその場で数える
  5. 現金 vs カード
     - カード決済がほぼ全域で通用する
     - 交通カードのチャージ・伝統市場・屋台は現金が必要
     - 端末で「円建て/ウォン建て」を選べたらウォン建てを選ぶ(DCC回避)
  6. WOWPASS・トラベルウォレットという選択肢
     ← 競合記事にはない項目。ここで差がつく
  7. FAQ
```

> **他社との差別化点**: 크리에이트립 환전 기사는 자사 예약 상품 유도 구조라 WOWPASS·트래블월렛 비교가 아예 없고, 표에 가격이 하나도 없다. Seoulmeguri는 판단 재료만 제공하고 결론은 독자에게 맡긴다.

---

## 7. 기술 스택

```
Framework     Next.js 15 (App Router)
Language      TypeScript (strict)
Styling       Tailwind CSS v4 (@theme でトークン定義)
Fonts         Zen Maru Gothic / Zen Kaku Gothic New / Outfit
Content       TypeScript files (content/areas/*.ts)
Deploy        Vercel
Analytics     Vercel Analytics (Cookie不要)
Lint          ESLint + Prettier
```

### 7.1 디렉터리

```
src/
  app/
    layout.tsx                 lang="ja" / metadata / fonts
    page.tsx                   トップ
    areas/[slug]/page.tsx
    guides/[slug]/page.tsx
    about/page.tsx
    opengraph-image.tsx
  components/
    hero/Hero.tsx
    weekday/WeekdayChecker.tsx  ← シグネチャ
    weekday/WeekdayTable.tsx
    course/MeguriLine.tsx       ← シグネチャ (SVG)
    course/StopCard.tsx
    area/AreaCard.tsx
    area/FitFilter.tsx          こんな人におすすめ/しない
    ui/PitfallBox.tsx
    ui/SpiceMeter.tsx
    ui/MapButton.tsx
    ui/UpdatedAt.tsx
    instagram/InstagramGrid.tsx
  content/
    types.ts
    areas/{city-hall,hannam,gangnam}.ts
    guides/exchange.ts
  lib/
    weekday.ts                 KST基準の曜日判定
    format.ts
styles/
  tokens.css                   CSS変数
```

### 7.2 요일 판정

```ts
// lib/weekday.ts
// ユーザーの端末TZに関係なく、韓国時間(KST/UTC+9)の曜日を返す。
// 日本もUTC+9なので実質同じだが、明示しておく。
export function getSeoulWeekday(d: Date = new Date()): Weekday {
  const kst = new Date(d.toLocaleString('en-US', { timeZone: 'Asia/Seoul' }));
  return (['sun','mon','tue','wed','thu','fri','sat'] as const)[kst.getDay()];
}
```

⚠️ SSG + 클라이언트 시각 조합 시 hydration mismatch 주의. 초기 렌더는 요일 미선택 상태로 두고, `useEffect` 로 오늘을 선택하는 방식을 권장.

---

## 8. SEO / 메타

```
lang           ja
hreflang       ja (単一言語のため x-default も ja)
title 例       ソウル 市庁・貞洞の1日コース｜曜日別の「行ける日」つき - Seoulmeguri
description    150字以内。「曜日」「行ける日」を必ず含める
OGP            エリアごとにキーカラーを反映した動的OG画像 (opengraph-image.tsx)
構造化データ     ItemList (コース) / FAQPage (両替ガイド) / Organization
sitemap.xml    next-sitemap or app/sitemap.ts
robots.txt     全許可
```

**狙うキーワード**
- `ソウル 月曜日 休み` ← 競合が手薄
- `北村 韓屋村 過怠料`
- `カロスキル 今`
- `漢南 ギャラリー 曜日`
- `韓国 両替 WOWPASS 比較`

---

## 9. 품질 기준 (Definition of Done)

- [ ] 모바일 375px 폭에서 가로 스크롤 없음
- [ ] Lighthouse: Performance ≥ 90 / Accessibility ≥ 95 (모바일)
- [ ] LCP < 2.5s (4G 스로틀)
- [ ] 모든 텍스트 대비 WCAG AA 충족
- [ ] 키보드만으로 曜日チェッカー 조작 가능. 포커스 링 가시
- [ ] `prefers-reduced-motion` 존중 (めぐりライン 애니메이션 스킵)
- [ ] 이미지 전부 `next/image` + 일본어 alt
- [ ] 모든 스팟에 NAVERマップ 링크
- [ ] 각 페이지 하단에 `更新日` 표시
- [ ] Footer에 免責文 (영업시간·요금은 변동 가능 / 방문 전 확인 권장)

---

## 10. 콘텐츠 운용 규칙

### 10.1 갱신 주기

| 항목 | 주기 | 이유 |
|---|---|---|
| 영업시간·휴관일 | 3개월 | 계절 변동 |
| 요금 | 6개월 | |
| 상권 상태(空室・閉店) | 6개월 | 가로수길 사례 |
| K-ETA 등 입국제도 | 수시 | 2026/12/31 만료 예정 |

### 10.2 서술 원칙

**① 저작권**
경쟁 매체의 문장을 번역·전재하지 않는다. 영업시간·주소·요금 같은 **사실 정보**는 자유롭게 쓸 수 있지만, 문장 구조와 표현은 반드시 새로 쓴다.

**② 역사 서술**
정동(旧公使館街・重明殿)과 도산공원(安昌浩)처럼 한일 근대사와 얽힌 장소가 코스에 포함된다.

- 사실은 **빼지 않는다**. 숨기면 오히려 불신을 산다
- 연도와 사실만 적고 **해석·감정은 붙이지 않는다**
- 예: 「1905年、この建物で乙巳条約が結ばれました」で止める

역사에 관심 있는 일본인 방문객은 실제로 정동을 찾는다. 담백한 서술이 그 층에게는 가장 읽을 만한 콘텐츠가 된다.

**③ 의료·미용**
v2에서 美容ページ를 만들 경우, 시술 효과나 특정 병원 추천은 다루지 않는다. **예약 절차 / 일본어 대응 여부 / 시술 후 주의사항 / 예약 리ードタイム** 등 절차 정보에 한정한다.

**④ 数字は出典とセットで**
「空室率43.9%」のような数字は、いつ・どこの調査かを併記する。これが他媒体との信頼差になる。

---

## 11. 로드맵

| 버전 | 내용 |
|---|---|
| **v1** | 톱 + 3에리어 + 両替ガイド + 曜日チェッカー |
| v1.1 | Instagram API 연동 / OG画像 자동생성 |
| v2 | 성수(팝업 캘린더) / 홍대 / 여의도 / 삼청 |
| v2.1 | 「今週のポップアップ」주간 갱신 코너 ← 성수의 무기 |
| v3 | 美容予約ガイド / 韓国語フレーズ集 / 曜日から探す検索 |

---

## 부록 A. 참고한 정보의 확인 필요 항목

작성 시점에서 확정하지 못했거나, 게재 전 재확인이 필요한 항목.

- [ ] ソウル市庁本館9階「空展望台」 — 2026년 7월 개방. 신규 시설이라 운영시간 변동 가능
- [ ] リウム美術館 디지털 가이드의 일본어 대응 여부
- [ ] タデウス・ロパック ソウル 의 현재 전시 기간과 개관일
- [ ] 各両替所の営業時間 (競合記事でも「訪問前確認」扱いの店舗が多い)
- [ ] 北村レッドゾーンの過怠料運用の現況
- [ ] K-ETA 免除の2027年以降の扱い
